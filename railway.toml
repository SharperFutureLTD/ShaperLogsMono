# Railway configuration for API server
[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build --workspace=@sharper-logs/api"

[deploy]
startCommand = "cd packages/api && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
